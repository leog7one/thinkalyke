$(function() {
    $('#new_comment').on('submit', function(e) {
        alert('You clicked submit!')
        e.preventDefault();

        //need url to submit post request
        //need the form data
    })
})